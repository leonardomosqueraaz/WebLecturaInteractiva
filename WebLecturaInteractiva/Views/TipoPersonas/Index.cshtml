@model IEnumerable<WebLecturaInteractiva.Models.TipoPersonas>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Crear", "Create", new {  }, new { @class = "btn btn-success", title = "Crear" })   
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre_TPersona)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion_TPersona)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado_Persona)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre_TPersona)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion_TPersona)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado_Persona)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.Id_TPersona }, new { @class = "btn btn-danger", title = "Editar" }) |
                @Html.ActionLink("Detalles", "Details", new { id = item.Id_TPersona }, new { @class = "btn btn-danger", title = "detalles" }) |
                @Html.ActionLink("Eliminar", "Delete", new { id = item.Id_TPersona }, new { @class = "btn btn-danger", title = "Eliminar" }) |
            </td>
        </tr>
}
    </tbody>
</table>
