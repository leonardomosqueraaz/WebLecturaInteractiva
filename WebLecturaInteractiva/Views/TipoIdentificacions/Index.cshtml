@model IEnumerable<WebLecturaInteractiva.Models.TipoIdentificacion>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombres_TipoIdentificacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion_TipoIdentificacion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombres_TipoIdentificacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion_TipoIdentificacion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_TipoIdentificacion">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_TipoIdentificacion">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_TipoIdentificacion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
